spring:
  profiles:
    active:
  application:
    name: website-core
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: actual_password_kekw
  jpa:
    hibernate:
      ddl-auto: validate
